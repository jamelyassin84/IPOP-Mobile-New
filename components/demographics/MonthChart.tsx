
import React, { FC } from 'react';
import { Text } from 'react-native';
import Container from '../../constants/Layout';

type Props = {};

const MonthChart: FC<Props> = ( props ) => {
    return (
        <Container>
            <Text>MonthChart</Text>
        </Container>
    );
};

export default MonthChart;
