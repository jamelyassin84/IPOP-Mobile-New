
import React, { FC } from 'react';
import { Text } from 'react-native';
import Container from '../constants/Layout';

type Props = {};

const Pyramid: FC<Props> = ( props ) => {
    return (
        <Container>
            <Text>Pyramid</Text>
        </Container>
    );
};

export default Pyramid;
