
import React, { FC } from 'react';
import { Text } from 'react-native';
import Container from '../../constants/Layout';

type Props = {};

const DistrictPicker: FC<Props> = ( props ) => {
    return (
        <Container>
            <Text>DistrictPicker</Text>
        </Container>
    );
};

export default DistrictPicker;
