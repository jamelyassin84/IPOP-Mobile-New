
import React, { FC } from 'react';
import { Text } from 'react-native';
import Container from '../../constants/Layout';

type Props = {};

const SummariesWithPercent: FC<Props> = ( props ) => {
    return (
        <Container>
            <Text>SummariesWithPercent</Text>
        </Container>
    );
};

export default SummariesWithPercent;
