
import React, { FC } from 'react';
import { Text } from 'react-native';
import Container from '../../../constants/Layout';

type Props = {};

const EmployemntStatus: FC<Props> = ( props ) => {
    return (
        <Container>
            <Text>EmployemntStatus</Text>
        </Container>
    );
};

export default EmployemntStatus;
