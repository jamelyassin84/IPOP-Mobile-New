
import React, { FC } from 'react';
import { Text } from 'react-native';
import Container from '../../../constants/Layout';

type Props = {};

const NumberOfCouples: FC<Props> = ( props ) => {
    return (
        <Container>
            <Text>NumberOfCouples</Text>
        </Container>
    );
};

export default NumberOfCouples;
