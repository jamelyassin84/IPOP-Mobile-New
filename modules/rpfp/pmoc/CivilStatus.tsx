
import React, { FC } from 'react';
import { Text } from 'react-native';
import Container from '../../../constants/Layout';

type Props = {};

const CivilStatus: FC<Props> = ( props ) => {
    return (
        <Container>
            <Text>CivilStatus</Text>
        </Container>
    );
};

export default CivilStatus;
