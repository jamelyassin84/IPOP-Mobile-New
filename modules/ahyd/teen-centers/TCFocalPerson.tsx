
import React, { FC } from 'react';
import { Text } from 'react-native';
import Container from '../../../constants/Layout';

type Props = {};

const TCFocalPerson: FC<Props> = ( props ) => {
    return (
        <Container>
            <Text>TCFocalPerson</Text>
        </Container>
    );
};

export default TCFocalPerson;
