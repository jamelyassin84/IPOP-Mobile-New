
import React, { FC } from 'react';
import { Text } from 'react-native';
import Container from '../../../constants/Layout';

type Props = {};

const TCPersonnelIncharge: FC<Props> = ( props ) => {
    return (
        <Container>
            <Text>TCPersonnelIncharge</Text>
        </Container>
    );
};

export default TCPersonnelIncharge;
