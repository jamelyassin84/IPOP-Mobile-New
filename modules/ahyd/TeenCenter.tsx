
import React, { FC } from 'react';
import { Text } from 'react-native';
import Container from '../../constants/Layout';

type Props = {};

const TeenCenter: FC<Props> = ( props ) => {
    return (
        <Container>
            <Text>TeenCenter</Text>
        </Container>
    );
};

export default TeenCenter;
