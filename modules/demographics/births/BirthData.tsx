
import React, { FC } from 'react';
import { Text } from 'react-native';
import Container from '../../../constants/Layout';

type Props = {};

const BirthData: FC<Props> = ( props ) => {
    return (
        <Container>
            <Text>BirthData</Text>
        </Container>
    );
};

export default BirthData;
