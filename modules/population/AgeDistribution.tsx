
import React, { FC } from 'react';
import { Text } from 'react-native';
import Container from '../../constants/Layout';

type Props = {};

const AgeDistribution: FC<Props> = ( props ) => {
    return (
        <Container>
            <Text>AgeDistribution</Text>
        </Container>
    );
};

export default AgeDistribution;
