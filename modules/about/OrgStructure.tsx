
import React, { FC } from 'react';
import { Text } from 'react-native';
import Container from '../../constants/Layout';

type Props = {};

const OrgStructure: FC<Props> = ( props ) => {
    return (
        <Container>
            <Text>OrgStructure</Text>
        </Container>
    );
};

export default OrgStructure;
